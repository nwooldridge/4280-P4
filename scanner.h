#ifndef SCANNER_H
#define SCANNER_H
#endif

#include "token.h"
#include <vector>

static bool filter1(char);
static std::vector<token> filter2(std::vector<token>);
std::vector <token> scanner(std::vector<char>);  
